const service=require("../services/survey.service");
async function submit(req,res){try{const r=await service.submit(Number(req.params.id),req.user.userId,req.body?.rating,req.body?.comment);const map={INVALID_RATING:[400,"Rating phải từ 1 đến 5"],NOT_ATTENDED:[403,"Chỉ người đã điểm danh mới được làm khảo sát"],ALREADY:[409,"Bạn đã gửi khảo sát cho Event này"]};if(map[r.code])return res.status(map[r.code][0]).json({success:false,message:map[r.code][1]});return res.status(201).json({success:true,message:"Gửi khảo sát thành công"});}catch(e){console.error(e);return res.status(500).json({success:false,message:"Không thể gửi khảo sát"});}}
async function mine(req,res){try{return res.json({success:true,data:await service.mine(Number(req.params.id),req.user.userId)});}catch(e){console.error(e);return res.status(500).json({success:false,message:"Lỗi hệ thống"});}}
async function list(req,res){try{const r=await service.list(Number(req.params.id),req.user);if(r.code==='NOT_FOUND')return res.status(404).json({success:false,message:'Không tìm thấy Event'});if(r.code==='FORBIDDEN')return res.status(403).json({success:false,message:'Bạn không có quyền xem khảo sát Event này'});return res.json({success:true,data:r.rows});}catch(e){console.error(e);return res.status(500).json({success:false,message:'Lỗi hệ thống'});}}
module.exports={submit,mine,list};
